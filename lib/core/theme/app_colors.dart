import 'package:flutter/material.dart';

class AppColors {
  // Основные цвета
  static const Color background = Color(0xFFF8F9FA);
  static const Color mainText = Color(0xFF333333);
  
  // Темный цвет (для верхней панели/кнопок)
  static const Color darkAccent = Color(0xFF2E2E35);
  
  // Акцентный цвет (кнопки, активные элементы)
  static const Color primaryAccent = Color(0xFF85C635);
  
  // Второстепенный текст (серый)
  static const Color secondaryText = Color(0xFF6C757D);
  
  // Линии и границы карточек
  static const Color border = Color(0xFFE9ECEF);
  
  // Вспомогательные цвета
  static const Color white = Colors.white;
  static const Color error = Colors.redAccent;
}
