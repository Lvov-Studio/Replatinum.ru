# Инструкции по сборке

- Не запускайте локальную сборку Flutter (`flutter build`).
- После изменений отправляйте коммиты в ветку `main`; GitHub Actions автоматически выполняет проверку и release-сборку APK.
