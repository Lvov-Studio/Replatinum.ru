# PlatinumStore App

Мобильное приложение для интернет-магазина [replatinum.ru](https://replatinum.ru).
Разработано на Flutter с использованием REST API (PHP, 1C-Bitrix).

## 🚀 Быстрый старт

### Разработка и отладка (Веб)
Для запуска в браузере (Chrome) с отключенной проверкой CORS (необходимо для работы с API на локальном этапе разработки):
```bash
flutter run -d chrome --web-browser-flag "--disable-web-security"
```

### Сборка релизного APK (Android)
Для сборки готового установочного файла `.apk` для Android-устройств:
```bash
# Для сохранения места на диске C, рекомендуется направить кэш сборки на диск D:
$env:GRADLE_USER_HOME="D:\gradle_cache"
flutter build apk --release
```
После завершения сборки, файл будет находиться по пути:
`build\app\outputs\apk\release\app-release.apk`

## 🔗 Структура проекта

* `lib/data/api/api_service.dart` — Основной класс для работы с REST API (Dio).
* `lib/data/models/` — Data-классы (модели) для парсинга JSON ответов.
* `lib/ui/screens/` — Основные экраны приложения (Главная, Каталог, Корзина, Профиль, Детальная карточка товара).
* `lib/ui/widgets/` — Переиспользуемые UI компоненты (Шапка, Слайдер, Карточки товаров).
* `lib/providers/` — Управление состоянием (например, `CartProvider` для корзины).

## ⚙️ Конфигурация API
Базовый URL для всех запросов жестко прописан в `ApiService`:
`https://replatinum.ru/local/api/mobile/v1/`

Все методы бэкенда и подробная документация по API описаны в файле:
`D:\Gemini\platinumstore_new\MOBILE_APP_LOG.md` (в репозитории бэкенда).

## 🎨 Дизайн и UI
Приложение строго следует дизайн-системе основного сайта:
* **Основной фон:** `#f8f9fa`
* **Основной текст:** `#333333`
* **Темные элементы (шапка/кнопки):** `#2e2e35`
* **Акцентный цвет:** `#85c635` (фирменный зеленый)

## 📞 Контакты в приложении
Телефон, указанный в шапке (используется для системного вызова):
`+78000000000` (Можно изменить в `lib/ui/widgets/custom_app_bar.dart` или `home_screen.dart`).

## Автоматическая сборка
Каждый push в ветку `main` запускает GitHub Actions: проверку кода и сборку release APK. Готовый APK доступен в артефактах завершённого запуска во вкладке Actions.

